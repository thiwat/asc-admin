export enum Entity {
  application = 'application',
  cms_block = 'cms_block',
  cms_page = 'cms_page',
  event = 'event',
  order = 'order',
  survey = 'survey',
  survey_answer = 'survey_answer',
  user = 'user',
}