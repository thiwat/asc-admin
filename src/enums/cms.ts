export enum CmsBlockType {
  blog = 'blog',
  carousel = 'carousel',
  html = 'html',
  image = 'image',
  button = 'button',
  items = 'items'
}

export enum CmsBlockButtonType {
  primary = 'primary',
  default = 'default'
}

export enum BlogSortBy {
  newest = 'newest',
  recommend = 'recommend'
}